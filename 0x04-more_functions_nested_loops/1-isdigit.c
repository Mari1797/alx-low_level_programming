
#include "main.h"


/**
 * _isdigit - checks for a digit through interval 0-9
 * @c: character to be checked
 *
 * Return: 1 if c is a digit or 0 otherwise
 */
int _isdigit(int x)
{
	if (c >= 0 && c <= 9)
	{
		return (1);
	}

	return (0);'
}
