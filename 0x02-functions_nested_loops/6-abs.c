#include "main.h"


/**
 * _abs - Computes bthe absolute value of an integer.
 * @c: The number to be computed
 *
 * Return: Absolute value of n or zerop
 */
int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	else
	{
		return (-c);
	}
}
