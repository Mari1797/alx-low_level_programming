#include "main.h"


/**
 * _islower - Checks for lower character
 * Return: 1 if c is a lower character or 0 if c is ptherwise
 * @c: character to be checked
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
